import { View } from "react-native";
import { Text, Box, Button, Heading } from "native-base";

const Login = ({ navigation }) => {
  return (
    <View style={{ flex: 1, alignItems: "center" }}>
      <Heading style={{ paddingTop: 10 }}>Bussin</Heading>
    </View>
  );
};

export default Login;
